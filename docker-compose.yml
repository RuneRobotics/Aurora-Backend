version: '3.8'

services:
  aurora:
    build: .
    ports:
      - "5800:5800"
      - "5801:5801"
    volumes:
      - .:/app
    devices:
      - "/dev/video0:/dev/video0"
      - "/dev/video1:/dev/video1"
    restart: unless-stopped
